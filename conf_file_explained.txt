server
{
	listen 						5000 //les différents ports sur lesquels on va écouter
    listen                      5001 //les différents ports sur lesquels on va écouter
    listen                      5002 //les différents ports sur lesquels on va écouter
    server_addr 				127.0.0.1 //l'adresse du serveur
	server_name 				hello.com //le nom du serveur
	root                        /Users/Charles/Desktop/42/test/zain/WS/Webserve-42 //le chemin d'accès vers la racine du serveur
    client_max_body_size  		3m //la taille maximale d'upload de fichier autorisé (le m veut dire MB). donc ici, 3 mb

    location /         //la localisation des pages web (à la racine)
    {
        index 					index.html //la page web de base (racine)
        http_methods  			GET  POST DELETE; //les méthodes http que reconnait le serveur. GET : requete pour read. renvoie 200 si c'est OK, 404 si not found, ou 400 par ex pour une bad request. POST : créer une nouvelle ressource subordonnée à une ressource parent et lui assigne un ID, renvoie 201 si successful. DELETE : supprime une ressource identifiée par son ID. renvoie 204 si successful. A lire : https://doc.oroinc.com/api/http-methods/
		root 					/Users/Charles/Desktop/42/test/zain/WS/Webserve-42/webpage/ //racine du dir de page web
        upload_dir 				/Users/Charles/Desktop/42/test/zain/WS/Webserve-42/webpage/uploads //chemin du dir d'uploads
    }
} 